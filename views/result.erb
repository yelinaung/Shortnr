<!DOCTYPE html>
<html>
<head>
  <title> Result </title>
  <link rel="stylesheet" href="http://yui.yahooapis.com/pure/0.2.0/pure-min.css">
</head>
<body>
  <h3> Success </h3>
  <a href="http://localhost:4567/<%= @token %>">http://localhost:4567/<%= @token %></a>
  <%= @redis.get("#{@token}") %>
  <% @redis.keys.each do |e| %>
    <a href="<%= e %>"><%= e %></a>
  <% end %>
</body>
</html>
